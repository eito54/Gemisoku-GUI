# Grosoq DESIGN.md

このアプリのUIデザイン原則。**すべてのUI変更はここに従うこと。**

## デザイン方向性: 「Racing HUD」

MK8DXの大会運営ツールという性質から、**密度高く・速度感があり・配信画面に映える**ダークUIを採る。
装飾は「レーシングHUD」のメタファーに限定し、汎用SaaS的な青紫グラデーションやカードモザイクは使わない。

## 色

| トークン | 値 | 用途 |
|---|---|---|
| `surface` | #0f172a | 画面ベース |
| `surface-raised` | #1e293b | パネル/カード |
| `surface-deep` | #020617 | 最背面 |
| `accent-{50..900}` | MK8DXレッド由来 | **プライマリ操作・ブランド強調のみ**。情報リンクにも使用可 |
| emerald / green | Tailwind既定 | 成功・接続状態専用 |
| purple | Tailwind既定 | セカンダリ操作（履歴系）のみ。乱用しない |
| text-slate-300〜200 | — | 本文（暗面パネル上でslate-500以下は禁止、CR≥4.5） |

- 青（blue-*）はカテゴリ識別（校正UIの列A/Bなど）以外で使用しない
- 影のglowは `rgba(239,68,68,x)` 系に統一（旧blue残存の混入を許さない）

## 書体

| 役割 | フォント | 用途 |
|---|---|---|
| display | Chakra Petch（JP fallback: Zen Kaku Gothic New） | h2/h3見出し・スコア数値・ブランド表記 |
| sans(既定) | Zen Kaku Gothic New → system-ui | UI本文 |

- 自己ホスト必須（@fontsource）。CDN読み込みは禁止
- 12px未満のテキストは禁止（バッジも12px）
- 丸ゴシック系・Russo One等の「玩具っぽい/安っぽい」書体は採用しない（2026-08-22フィードバック）

## スペース / 半径

- スペースは4pxグリッド（gap/padding: 4/8/12/16/24）。`p-[30px]`等の直書き禁止
- 半径は `rounded-lg`(8) / `rounded-xl`(12) / `rounded-2xl`(16) / `rounded-full` の4種のみ
- レイアウト寸法（ロゴ幅等）としての任意値は許容

## スクロール構造

- **スクロールバーはウィンドウで1本**（メインコンテンツ領域のみ）
- パネル内部に`overflow-y-auto`を作らない。例外はモーダル内（高さ制約が必須な場合のみ）
- 長大リストはmax-heightで区切らず自然に流す

## モーション

- 目的のない装飾アニメーション禁止。「階層を示す/状態を伝える」ためだけに動かす
- スプラッシュは最短1.4秒・クリックで即スキップ可能
- `prefers-reduced-motion` を必ず尊重（main.cssのグローバルkillを維持）

## コピー

- 見出し・ボタンに絵文字を使用しない（lucideアイコンを使用）
- エラー文は「何が起き、どうすればいいか」を日本語で明示
- 「If deleting 30% of the copy improves it, keep deleting」

## アクセシビリティ最低基準

- 本文コントラスト CR ≥ 4.5（暗面上のslate-500禁止）
- クリック領域 ≥ 32×32px
- `:focus-visible` を全要素で可視化（main.cssのグローバル定義を維持）
